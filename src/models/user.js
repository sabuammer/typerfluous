export default class User {
    constructor(username, email, userId, photoURL) {
        this.username = username;
        this.email = email;
        this.userId = userId;
        this.photoURL = photoURL;
    }
}